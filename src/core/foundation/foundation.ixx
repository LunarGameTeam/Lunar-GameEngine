
#include "core/core_config.h"

export module Luna;

// Module µ—È–‘


export CORE_API size_t SizeAligned2Pow(const size_t& size_in, const size_t& size_aligned_in);
export CORE_API size_t SizeAligned(const size_t& size_in, const size_t& size_aligned_in);